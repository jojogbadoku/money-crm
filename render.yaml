services:
  - type: web
    name: money-crm
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: APP_PIN
        sync: false
      - key: SESSION_SECRET
        generateValue: true
